{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Debugger",
            "request": "launch",
            "command": "yarn debug",
            "cwd": "${workspaceFolder}"
        }
    ]
}